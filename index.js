const app = {};

app.config = {
  'timeBetweenJokes': 1000
};

// TODO: write a function that prints a random joke
app.printAJoke = function() {

}

// TODO: write a function that calls printJoke infinitely
app.infiniteLoop = function() {

}

// and finally invoke the loop
app.infiniteLoop();
